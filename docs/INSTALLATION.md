# Installation

1. Node.js 20 oder neuer installieren.
2. Repository klonen.
3. `npm test` ausführen.
4. `npm start` ausführen.
5. Browser unter `http://localhost:3000` öffnen.

Docker-Alternative: `docker compose up --build`.
